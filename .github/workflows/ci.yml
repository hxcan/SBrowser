name: CI Build

on:
  push:
    branches: ['**']
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          qt6-base-dev \
          cmake \
          g++ \
          make \
          libgl1-mesa-dev \
          libx11-dev \
          libglu1-mesa-dev \
          zlib1g-dev
    
    - name: Run qmake6
      run: |
        q6make6 sbrowser.pro
    
    - name: Build project
      run: |
        make -j$(nproc)
    
    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      if: success()
      with:
        name: SBrowser-build
        path: 
        - SBrowserCppWebEngine/
        retention-days: 7
